import { StyleSheet, Text, View } from 'react-native'
import React from 'react';
import RestaurantItems from '../components/RestrurentItems'

const BigChain = () => {
  return (
    <View>
       <RestaurantItems />
    </View>
  )
}

export default BigChain

const styles = StyleSheet.create({})    